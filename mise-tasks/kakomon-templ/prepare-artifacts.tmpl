#!/usr/bin/env bash
# Source skeleton only. Copy to mise-tasks/__KAKOMON_SLUG__/prepare-artifacts,
# remove this guard, replace every TODO/__...__, add #MISE description, then make executable.
set -euo pipefail
echo "error: prepare-artifacts.tmpl is a non-runnable source skeleton" >&2
exit 64

TARGET_SLUG="__KAKOMON_SLUG__"

# Copy this optional task only when the target has a local inspection artifact
# boundary. The entrypoint must pin download/build inputs and emit manifests
# and digests under a gitignored target-owned dist directory.
TODO_prepare_target_artifacts "${TARGET_SLUG}/scripts/__PREPARE_ARTIFACTS_ENTRYPOINT__"
