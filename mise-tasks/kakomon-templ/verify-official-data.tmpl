#!/usr/bin/env bash
# Source skeleton only. Copy to mise-tasks/__KAKOMON_SLUG__/verify-official-data,
# remove this guard, replace every TODO/__...__, add #MISE description, then make executable.
set -euo pipefail
echo "error: verify-official-data.tmpl is a non-runnable source skeleton" >&2
exit 64

TARGET_SLUG="__KAKOMON_SLUG__"

# Verify the worktree-local official mirror identity before checking the
# target-owned file manifest. Do not fetch or mutate the mirror here.
TODO_verify_official_mirror_url_and_full_sha "${TARGET_SLUG}"
TODO_verify_target_file_manifest "${TARGET_SLUG}/__OFFICIAL_DATA_MANIFEST__"
