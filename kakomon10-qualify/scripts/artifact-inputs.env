# KAKOMON10-QUALIFY artifact build inputs. Keep binary artifacts out of Git;
# this file records the target-scoped frontend Release selector consumed by
# provisioning/60-initdb.sh (FRONTEND_RELEASE_TAG) and, once a Packer template
# exists, the AMI build task. No source AMI ID is recorded yet: Packer has not
# been authored or run in this session (see ../packer/README.md).
KAKOMON10_QUALIFY_FRONTEND_RELEASE_TAG=latest
