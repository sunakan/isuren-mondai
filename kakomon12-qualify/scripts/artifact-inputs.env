# shellcheck shell=bash
# Exact artifact inputs. This file is sourced by target-local scripts only.
OFFICIAL_REPOSITORY_URL=https://github.com/isucon/isucon12-qualify.git
BASE_AMI_ID=ami-0df1235688731e6cc
BASE_AMI_OWNER=099720109477
BASE_IMAGE_SERIAL=20260806
OFFICIAL_COMMIT=95958774bde66d47fd26cc820fd2fbfbece798f1
OFFICIAL_LICENSE=MIT
OFFICIAL_COPYRIGHT=Copyright\ \(c\)\ 2022\ ISUCON

# initial_data is distributed only as a GitHub Release asset (bench/Makefile's
# `gh release list | awk '/Latest/{print $3}' | xargs gh release download`),
# never as a Git blob. Resolved during the `verify` mode external-operation
# preflight: `gh release list --repo isucon/isucon12-qualify` showed this tag
# as `Latest`; the asset was downloaded via the GitHub web UI (this sandbox's
# `gh`/`ghtkn` token could list releases via GraphQL but got 403/"release not
# found" on the REST releases/tags and download endpoints) and its SHA-256 was
# verified independently by both the human and this session against the same
# downloaded file.
INITIAL_DATA_RELEASE_TAG=data/20220712_1505-745a3fdfb5783afc048ecaebd054acd20151872d
INITIAL_DATA_ASSET_NAME=initial_data.tar.gz
INITIAL_DATA_SHA256=024fc58d1468ac827caf023d0bfdf0e7375fe1d0ce9dea78a2fb3bedf5786506
